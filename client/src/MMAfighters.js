import React from 'react'

const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }
}

const MMAfighters = () =>
  <div style={styles.container}>
    <h3>MMA Fighters</h3>
  </div>

export default MMAfighters
