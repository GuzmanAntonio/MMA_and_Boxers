import React from 'react'

const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    color: '#ECF0F1',
    textShadow: '2px 4px 8px #000000'
  }
}

const Home = () =>
  <div style={styles.container}>
    <h2>Home</h2>
  </div>

export default Home
