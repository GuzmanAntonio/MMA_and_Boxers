import React from 'react'

const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }
}

const Home = () => 
  <div style={styles.container}>
    <h3>Home</h3>
  </div>

export default Home
